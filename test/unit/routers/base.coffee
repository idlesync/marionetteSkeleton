application = require 'application'

describe 'base router', ->
  baseRouter = undefined
  BaseRouter = undefined

  beforeEach ->
    BaseRouter = require 'routers/Base'

  afterEach ->

  it.skip 'adds a `navigate:link` event listener to application', ->

  it.skip 'events cause navigation to a link', ->
